# InboxBridge Safari Extension

This target folder is reserved for the Safari extension packaging phase.

Planned responsibilities:

- Safari-specific packaging/export workflow
- Apple platform wrapper/project files when needed
- signing and distribution notes for Safari
- any target-local adapters that should stay separate from Chromium and Firefox

The shared runtime logic should continue to live in `../shared/`.
